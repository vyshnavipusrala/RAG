## Chunk Enhancement Techniques in RAG

**Chunk enhancement** = Improving chunks before storing them in the vector database to improve retrieval quality.

1. **Contextual Chunking**

   * Adds surrounding context to make chunks more understandable.

2. **Contextual Retrieval**

   * Adds document title, section, or background information to each chunk.

3. **Metadata Enrichment**

   * Adds metadata like **document name, page, section, date, and department**.

4. **Chunk Summarization**

   * Creates a short summary of each chunk for better semantic matching.

5. **Hypothetical Questions**

   * Generates possible questions that the chunk can answer and uses them for retrieval.

6. **Parent-Child Chunking**

   * Uses small child chunks for precise retrieval and larger parent chunks to provide context.

7. **Chunk Overlap**

   * Overlaps neighboring chunks to preserve context across boundaries.
  
## Common Chunking Methods in RAG

**Chunking** = Splitting large documents into smaller pieces so they can be efficiently embedded and retrieved.

1. **Fixed-Size Chunking**

   * Splits text into chunks of a fixed number of tokens/characters.
   * Simple and fast.
   * Example: 500 tokens with 50-token overlap.

2. **Recursive Character Chunking**

   * Splits text using separators such as paragraphs, sentences, and spaces.
   * Tries to keep related text together.
   * Commonly used in RAG.

3. **Sentence-Based Chunking**

   * Splits documents based on complete sentences.
   * Helps preserve sentence-level meaning.

4. **Paragraph-Based Chunking**

   * Splits text according to paragraphs.
   * Useful when each paragraph contains a complete idea.

5. **Semantic Chunking**

   * Splits text based on **meaning/topic changes** rather than fixed size.
   * Keeps semantically related information together.

6. **Document/Structure-Based Chunking**

   * Uses document structure such as **headings, sections, chapters, and subsections**.
   * Useful for structured documents and technical documentation.

7. **Parent-Child Chunking**

   * Creates smaller child chunks for precise retrieval and larger parent chunks for context.
   * Retrieve child → provide parent context to the LLM.

### Remember:

> **Fixed-size → Simple**
> **Recursive → Flexible**
> **Sentence/Paragraph → Natural boundaries**
> **Semantic → Meaning-based**
> **Structure-based → Document organization**
> **Parent-child → Precision + context**


8. **Semantic Chunking**

   * Splits text based on **meaning and topic** rather than only fixed token size.

### Remember:

> **Better chunks + better context → better retrieval → better RAG answers.**

## Criteria to Choose a Chunking Method in RAG

The chunking method should be selected based on the **document type, content structure, query type, and RAG requirements**.

1. **Document Type**

   * PDFs, web pages, code, tables, FAQs, and legal documents may need different strategies.

2. **Document Structure**

   * If documents have clear headings/sections → use **structure-based chunking**.
   * For unstructured text → **recursive or semantic chunking** may work better.

3. **Query Type**

   * Simple factual queries → smaller chunks may be sufficient.
   * Complex questions → larger or parent-child chunks can preserve more context.

4. **Context Requirements**

   * If information spans multiple sentences/paragraphs → use **overlap or larger chunks**.

5. **Retrieval Precision**

   * Smaller chunks generally provide more precise retrieval.
   * Avoid chunks that are so small that they lose necessary context.

6. **Semantic Coherence**

   * Keep related information together.
   * **Semantic chunking** is useful when meaning is more important than fixed size.

7. **Latency and Cost**

   * More/smaller chunks increase the number of embeddings and retrieval operations.
   * Larger chunks increase the amount of context sent to the LLM.

8. **Evaluation**

   * Test different chunking strategies and compare:

     * **Context Precision**
     * **Context Recall**
     * **Answer Relevance**
     * **Faithfulness**
     * Latency and cost

### Remember:

> **Document type + Structure + Query type + Context needs + Retrieval quality + Cost/Latency → Choose the chunking method.**

## common retrieval approaches in rag
Common retrieval approaches in RAG systems include **dense retrieval, sparse retrieval, hybrid retrieval, multi-query retrieval, MMR retrieval, and metadata-based retrieval**.

* **Dense Retrieval** uses embeddings to capture semantic similarity, helping match queries with relevant document chunks based on meaning.

* **Sparse Retrieval** uses traditional keyword-based methods such as **TF-IDF and BM25**, focusing on exact keyword matching. It is useful for names, IDs, technical terms, and specific keywords.

* **Hybrid Retrieval** combines dense and sparse retrieval to leverage both **semantic understanding and keyword matching**, improving overall retrieval quality.

* **Multi-Query Retrieval** generates multiple variations of the user's query and retrieves documents for each variation. This helps find relevant information that may be missed by a single query.

* **MMR (Maximal Marginal Relevance)** retrieves documents that are both **relevant and diverse**, reducing duplicate or highly similar chunks.

* **Metadata Filtering** uses attributes such as **document type, date, department, category, or access level** to restrict the search to relevant documents.

* **Parent-Child Retrieval** retrieves a smaller child chunk for precise matching but provides the larger parent chunk to the LLM for additional context.

* **Re-ranking** retrieves an initial set of candidate documents and then uses a reranker to **reorder them based on their relevance** to the query.

These retrieval approaches help RAG systems retrieve **relevant, diverse, and contextually useful information**, improving the quality and grounding of generated responses.

## Key Metrics for Evaluating Retrieval Quality in RAG

Retrieval evaluation measures **how effectively the retriever finds relevant information** for a user's query.

* **Context Precision**

  * Measures whether the retrieved chunks are **relevant** to the query.
  * Higher precision means fewer irrelevant chunks are retrieved.

* **Context Recall**

  * Measures whether the retriever finds **all the important information** needed to answer the query.
  * Higher recall means fewer relevant pieces of information are missed.

* **Hit Rate**

  * Measures whether at least one relevant document appears in the retrieved results.
  * Example: If the correct document is retrieved in the top-k results, it is a hit.

* **MRR (Mean Reciprocal Rank)**

  * Measures how highly the **first relevant document** appears in the results.
  * Higher MRR means relevant results appear closer to the top.

* **Precision@K**

  * Measures the proportion of relevant documents among the **top K retrieved documents**.

* **Recall@K**

  * Measures how many of the relevant documents are found within the **top K results**.

* **NDCG (Normalized Discounted Cumulative Gain)**

  * Measures the quality of ranking by giving **higher importance to relevant documents appearing near the top**.

### Remember:

> **Precision → Are retrieved documents relevant?**
> **Recall → Did we retrieve the necessary information?**
> **Hit Rate → Did we find at least one relevant result?**
> **MRR → How high is the first relevant result?**
> **NDCG → How good is the overall ranking?**



