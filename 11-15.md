## How to Choose Chunk Size in RAG

* There is **no fixed optimal chunk size**; it depends on the document type, query complexity, and use case.
* **Too small:** important context may be lost and information becomes fragmented.
* **Too large:** retrieval becomes less precise and increases LLM token usage, latency, and cost.
* Consider the **document structure** — use section/paragraph-based chunking when possible.
* Use **chunk overlap** to preserve context between consecutive chunks.
* Start with around **400–700 tokens** and **10–20% overlap** for general text.
* Experiment with different sizes and evaluate using **Context Precision, Context Recall, Answer Relevance, and Faithfulness**.
* Choose the size that provides the best balance of **retrieval quality, context, latency, and cost**.

**Remember:**

> **Too small → loses context | Too large → loses precision | Optimal → balanced retrieval**

# Key Considerations When Choosing an LLM for RAG

* **Context window:** The model should support enough tokens to handle the retrieved chunks and conversation history.

* **Accuracy & reasoning:** It should understand the retrieved context and generate reliable, relevant answers.

* **Instruction following:** Important for making the LLM answer **only from retrieved context** and avoid unnecessary hallucination.

* **Latency:** Choose a model that can respond quickly enough for your application.

* **Cost:** Consider input/output token pricing, especially for high-volume applications.

* **Domain performance:** Test how well the model handles your specific domain, such as finance, healthcare, legal, or technical documents.

* **Multilingual support:** Important if users or documents contain multiple languages.

* **Deployment requirements:** Decide between **cloud APIs** and **self-hosted/open-source models** based on privacy, infrastructure, and compliance requirements.

* **Integration:** Ensure the model works well with your RAG framework, tools, and APIs.

* **Scalability:** Consider whether it can handle increasing numbers of users and requests.

### Remember:

> **Choose an LLM based on Quality + Context Window + Latency + Cost + Domain Performance + Deployment Requirements.**

## Popular RAG Frameworks

* **LangChain** – Framework for building LLM and RAG applications.
* **LlamaIndex** – Focuses on connecting LLMs with external/private data and retrieval.
* **Haystack** – Used for building production-ready search and RAG pipelines.
* **Microsoft GraphRAG** – Useful for retrieval using relationships and knowledge graphs.
* **Semantic Kernel** – Framework for integrating LLMs with enterprise applications.

### Why LangChain?

* Easy integration with **LLMs, embeddings, vector databases, and retrievers**.
* **Modular and flexible** architecture.
* Supports advanced RAG techniques like **hybrid search, multi-query, reranking, and metadata filtering**.
* Large ecosystem with many integrations.
* Works well with **LangGraph** for complex workflows.
* Suitable for both **prototyping and production**.

**Remember:**

> **LangChain = Flexible + Modular + Many Integrations + Advanced RAG Support**

## Query Transformation Techniques in RAG

**Purpose:** Transform the user's query to improve retrieval of relevant information.

1. **Query Rewriting**

   * Rephrases the query to make it clearer and more searchable.

2. **Query Expansion**

   * Adds related keywords, synonyms, or terms to the query.

3. **Multi-Query Generation**

   * Generates multiple variations of the same query to retrieve diverse relevant documents.

4. **HyDE (Hypothetical Document Embeddings)**

   * Generates a hypothetical answer/document and uses its embedding for retrieval.

5. **Query Decomposition**

   * Breaks a complex query into smaller sub-queries.

6. **Step-Back Query**

   * Converts a specific question into a broader conceptual question to improve retrieval.

7. **Conversational Query Rewriting**

   * Uses chat history to convert follow-up questions into standalone queries.

### Remember:

> **Query Transformation = Modify the query → Better retrieval → Better context → Better answer**

## HyPE in RAG

* **HyPE = Hypothetical Prompt Embeddings.**
* It improves retrieval by generating **hypothetical questions/prompts** for documents.
* These hypothetical prompts are converted into **embeddings** and stored with the corresponding documents.

### Workflow

```text
Documents
   ↓
Generate hypothetical questions/prompts
   ↓
Create embeddings
   ↓
Store in vector database
   ↓
User Query
   ↓
Similarity Search
   ↓
Relevant Documents
   ↓
LLM → Final Answer
```

### Example

Document:

> "Employees need manager approval before requesting VPN access."

Generated hypothetical prompt:

> "Is manager approval required for VPN access?"

A similar user query can then match this prompt and retrieve the correct document.

### HyDE vs HyPE

* **HyDE:** Enriches the **query** using a hypothetical answer/document.
* **HyPE:** Enriches the **document/index** using hypothetical questions/prompts.

**Remember:**

> **HyDE → Query enhancement**
> **HyPE → Index/document enhancement**






